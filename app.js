const paths={overwhelmed:{title:"Make the moment smaller",note:"You do not need to solve the whole situation. First, reduce the number of things your mind is carrying.",steps:["Name the one pressure that is loudest","Choose what can wait until tomorrow","Take one two-minute action"]},stuck:{title:"Find the next visible step",note:"Being stuck often means the decision is too large or the options are still tangled together.",steps:["Write the decision in one sentence","Remove any option you cannot actually choose","Test the smallest reversible move"]},disconnected:{title:"Return to something real",note:"Before interpreting everything, reconnect with your body, surroundings, or one safe person.",steps:["Notice five concrete things around you","Change room, light, temperature, or posture","Send one honest sentence to someone safe"]}};
document.querySelectorAll(".choice").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll(".choice").forEach(x=>x.classList.remove("active"));button.classList.add("active");const path=paths[button.dataset.path];document.querySelector("#path-title").textContent=path.title;document.querySelector("#path-note").textContent=path.note;document.querySelector("#path-steps").innerHTML=path.steps.map(x=>`<li>${x}</li>`).join("")}));
const menu=document.querySelector(".menu-button"),nav=document.querySelector(".nav");menu.addEventListener("click",()=>nav.classList.toggle("open"));nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));
if("serviceWorker" in navigator)navigator.serviceWorker.register("sw.js").catch(()=>{});if(matchMedia("(display-mode: standalone)").matches)document.querySelector("#install-title").textContent="Nobody's Simple is installed";
